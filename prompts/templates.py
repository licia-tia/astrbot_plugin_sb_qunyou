"""
Prompt 模板集中管理

所有发送给 LLM 的 prompt 模板都定义在这里，便于审计和维护。
SL 的 prompt 分散在 20+ 文件中，这里统一收口。
"""

# -----------------------------------------------------------------------
# 群画像学习
# -----------------------------------------------------------------------

GROUP_PERSONA_LEARN = """你是一位群聊分析专家。请根据以下群聊消息，提取出这个群的核心特征。

要求：
1. 用客观的第三人称描述群聊氛围
2. 提取 3-5 个核心关注点/热点话题
3. 描述群内互动风格（是否活跃、正式/informal、是否有明显的群文化）
4. 不要包含任何个人隐私信息

消息记录：
{messages}

请输出群画像描述："""

# -----------------------------------------------------------------------
# 话题摘要
# -----------------------------------------------------------------------

THREAD_SUMMARY = """请用 2-4 个关键词概括以下对话的主题：

{messages}

主题关键词："""

# -----------------------------------------------------------------------
# 情绪分析
# -----------------------------------------------------------------------

EMOTION_ANALYZE = """分析以下对话中用户对 Bot 表达的情感倾向。

对话内容：
{message}

请从以下选项中选择最匹配的情绪标签，仅输出标签：
happy, neutral, angry, sad, excited, bored

情绪标签："""

# -----------------------------------------------------------------------
# 用户记忆抽取
# -----------------------------------------------------------------------

MEMORY_EXTRACT = """从以下消息中抽取值得记忆的事实（如果有的话）。
请只抽取客观事实或明确的偏好声明，不要推测。
如果没有值得记忆的内容，输出空的 JSON 数组。

发送者：{sender_name}
消息：{message}

请以 JSON 数组格式输出，每个元素是一句简短的事实描述：
```json
["事实1", "事实2"]
```"""

# -----------------------------------------------------------------------
# 黑话含义推断（批量）
# -----------------------------------------------------------------------

JARGON_INFER_BATCH = """以下是一个群聊中频繁出现的词语或短语。
请为每个词语推断其在群聊语境下的含义。

词语列表：
{terms}

请以 JSON 对象格式输出，key 为词语，value 为含义（一句话，不超过 30 字）：
```json
{{"词语1": "含义1", "词语2": "含义2"}}
```"""

# -----------------------------------------------------------------------
# 语义完整性判断 (L2 防抖)
# -----------------------------------------------------------------------

SEMANTIC_COMPLETENESS = """判断以下文本是否表达了一个完整的意图或想法。

文本：{text}

如果是完整意图，输出 "complete"
如果是不完整的片段（如只有称呼、语气词、或明显话说一半），输出 "incomplete"

判断："""

# -----------------------------------------------------------------------
# 上下文注入模板
# -----------------------------------------------------------------------

INJECTION_GROUP_PERSONA = """<group_persona>
{persona}
</group_persona>"""

INJECTION_EMOTION = """<emotion_state>
当前情绪：{mood}
</emotion_state>"""

INJECTION_THREAD_CONTEXT = """<thread_context topic="{topic}">
{messages}
</thread_context>"""

INJECTION_USER_MEMORIES = """<user_memories user="{user_id}" trust="medium">
{memories}
</user_memories>"""

INJECTION_JARGON = """<jargon_hints trust="low">
{hints}
</jargon_hints>"""

COMBINED_LEARNING_PROMPT = """你是一位群聊人格分析专家。请根据以下信息，更新该群的人格描述。

原始人格设定：
{original_persona}

群聊消息（最近 {count} 条）：
{messages}

要求：
1. 提炼群的核心定位和话题
2. 总结群成员的说话风格/语气特征（如口头禅、常用句式、表达习惯）
3. 描述群文化/潜规则
4. 如果有原始人格，请在原有基础上迭代演化，保留核心特征
5. 不要包含个人隐私信息

人格描述："""
